#!/usr/bin/env bash

if pgrep -x "wlsunset" > /dev/null; then
    pkill -x "wlsunset"
    notify-send -a 'osd' -c 'osd-text-small' \
                -h string:x-canonical-private-synchronous:nightlight \
                -t 2000 "nightlight off"
else
    wlsunset -t 3500 -T 3501 &
    
    notify-send -a 'osd' -c 'osd-text-small' \
                -h string:x-canonical-private-synchronous:nightlight \
                -t 2000 "nightlight on"
fi
